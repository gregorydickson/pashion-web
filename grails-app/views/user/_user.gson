import pashion.User


model {  
    User user
}

json g.render(user,[expand:['connections']]){
	pressHouse {
		name user.pressHouse?.name
		id user.pressHouse?.id
	}

	brand {
		name user.brand?.name
		id user.brand?.id
	}
}